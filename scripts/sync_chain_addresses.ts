// scripts/sync_chain_addresses.ts
//
// Pulls the canonical ERC-8004 chain registry from
// https://github.com/erc-8004/erc-8004-contracts/blob/master/scripts/addresses.ts
// and updates `crates/core/src/chains.rs`.
//
// Run manually when the spec ships a new mainnet:
//   npx tsx scripts/sync_chain_addresses.ts
//
// Bootstrap placeholder — real implementation lands in the multi-chain PR.

console.error('sync_chain_addresses: not yet implemented (multi-chain dispatcher arrives in v1.x)');
process.exit(0);
