// Auth.js v5 wiring. Real configuration lands in Phase 2 once we register the
// GitHub OAuth App and provision AUTH_SECRET in Vercel.
//
// Plan §8.1: GitHub provider, JWT session strategy, callback at
// /api/auth/callback/github.

export const AUTH_PLACEHOLDER = true;
