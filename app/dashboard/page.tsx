export default function DashboardHome() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: 40 }}>
      <h1>Dashboard</h1>
      <p>Sign in with GitHub to issue an API key. (Auth.js wiring lands in Phase 2.)</p>
    </main>
  );
}
